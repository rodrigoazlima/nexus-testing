{
  "name": "nexus-image-pipeline-tests",
  "version": "0.1.0",
  "private": true,
  "description": "Black-box Playwright tests for the Nexus Campaigns image ingestion pipeline (vault filesystem + dashboard UI).",
  "scripts": {
    "test": "tsx scripts/run-playwright.ts --grep-invert @slow-agent",
    "test:keep": "tsx scripts/run-playwright.ts --grep-invert @slow-agent --keep",
    "test:only": "tsx scripts/run-playwright.ts --only",
    "test:pipeline:fast": "tsx scripts/run-playwright.ts tests/pipeline --grep-invert @slow-agent",
    "test:pipeline:slow": "tsx scripts/run-playwright.ts tests/pipeline --grep @slow-agent --workers=4",
    "test:unit": "node --import dotenv/config --import tsx --test tests/helpers/*.test.ts",
    "report": "playwright show-report",
    "clean": "tsx scripts/clean.ts",
    "lint": "eslint ."
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "@types/node": "^22.0.0",
    "dotenv": "^17.4.2",
    "eslint": "^10.6.0",
    "systeminformation": "^5.31.17",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "typescript-eslint": "^8.63.0"
  },
  "dependencies": {
    "gray-matter": "^4.0.3"
  }
}
